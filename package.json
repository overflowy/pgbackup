{
  "name": "pgbackup",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "compile": "bun build ./src/index.ts --compile --outfile ./dist/pgbackup",
    "pull": "git reset --hard HEAD && git pull --rebase"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.7",
    "ora": "^8.1.1"
  }
}